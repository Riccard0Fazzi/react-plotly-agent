services:
  ollama:
    image: ollama/ollama:latest
    container_name: react-plotly-ollama
    ports:
      - "11435:11434"
    volumes:
      - ollama_models:/root/.ollama

volumes:
  ollama_models:
